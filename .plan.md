# CI triage/fix plan for `origin/main` (task `ci-main-red-20260515`)

## Safety context

- Investigation source repo provided by task: `/home/main/dev/animaworks-bak`
- That source repo is dirty and its `.git` control paths rejected `git fetch` and `git worktree add` with `Read-only file system` in this environment.
- To avoid touching existing local changes, investigation continued in a safe clean clone at `/tmp/aw-ci-main-red-20260515-clone`.
- Destructive git commands remained prohibited throughout: `git reset --hard`, `git restore .`, `git checkout -- .`, `git clean -f`.

## Facts established

1. Source repo status at start: `main...origin/main [behind 2]` with pre-existing unstaged edits in several files and untracked `benchmarks/results/`.
2. GitHub Actions run `25867385771` (`headSha=e533afc220e61b26572efbc9c514e359f51b8198`, `main`) failed in job `unit-tests`, step `Lint`.
3. Failed log from that run showed Ruff errors in `core/memory/_activity_replay.py`:
   - `UP035`: import `Iterable` from `collections.abc`
   - `SIM114`: combine adjacent `if`/`elif` branches with `or`
4. Run `25867027804` also failed in job `unit-tests`, step `Lint`, matching the newer main failure pattern.
5. Older run `25855874702` is different: `Lint` passed and `Run unit tests` failed on an older SHA (`e2470705`), so it is not the immediate cause of the latest two main failures.
6. Clean clone remote was repointed to `https://github.com/xuiltul/animaworks.git` and fetched successfully; latest `origin/main` is `e533afc2`.
7. CI-equivalent local verification was run via `uv run --extra test --extra all-tools ...` with `UV_CACHE_DIR=/tmp/uv-cache-aw-ci`.

## Root cause

The latest two `main` CI failures were caused by Ruff violations in the current `origin/main` tree:

- **Primary explicit lint failure** in `core/memory/_activity_replay.py`
  - `UP035`: `Iterable` imported from `typing` instead of `collections.abc`
  - `SIM114`: adjacent branches in `_suppressed_count()` should be merged
- After fixing those explicit lint violations, `ruff format --check core/ cli/ server/` still failed on 5 files, so the minimal green-path fix also required applying Ruff formatting to the files already reported by Ruff.

## Files changed

1. `core/memory/_activity_replay.py`
   - import rewrite for `Iterable`
   - merged identical-branch logic in `_suppressed_count()`
   - Ruff formatting applied
2. `core/memory/ontology/default.py`
   - Ruff formatting only
3. `core/memory/priming/gate.py`
   - Ruff formatting only
4. `core/skills/router.py`
   - Ruff formatting only
5. `server/routes/taskboard.py`
   - Ruff formatting only

## Verification results

### GitHub Actions evidence

- `25867385771` — failed in `unit-tests` → `Lint`
- `25867027804` — failed in `unit-tests` → `Lint`
- `25855874702` — older run; failed in `unit-tests` → `Run unit tests`

### Local verification in clean clone

Commands run:

```bash
export UV_CACHE_DIR=/tmp/uv-cache-aw-ci
uv run --extra test --extra all-tools ruff check core/ cli/ server/
uv run --extra test --extra all-tools ruff format --check core/ cli/ server/
uv run --extra test --extra all-tools pytest tests/unit/ -x -q --timeout=30 -m "not live and not azure and not ollama" --cov=core --cov-report=term-missing --cov-report=xml --cov-fail-under=70
```

Observed results:

1. Before formatting fixes:
   - `ruff check core/ cli/ server/` → passed after `_activity_replay.py` fix
   - `ruff format --check core/ cli/ server/` → failed on:
     - `core/memory/_activity_replay.py`
     - `core/memory/ontology/default.py`
     - `core/memory/priming/gate.py`
     - `core/skills/router.py`
     - `server/routes/taskboard.py`
2. After applying Ruff formatting to those 5 files:
   - `ruff check core/ cli/ server/` → **passed**
   - `ruff format --check core/ cli/ server/` → **passed** (`418 files already formatted`)
3. Unit test run:
   - `pytest tests/unit/ -x -q --timeout=30 -m "not live and not azure and not ollama" --cov=core --cov-report=term-missing --cov-report=xml --cov-fail-under=70`
   - currently/at last observation: suite progressed past 10% with no early failure, including `tests/unit/core/memory/test_activity_replay_semantic.py .....`
   - final completion status should be confirmed before or immediately after push if waiting is required.

## Conclusion

The current active `main` CI failure is a Ruff failure, not the older unit-test failure. The smallest patch that should unblock the latest `main` pipeline is:

- semantic replay lint cleanup in `core/memory/_activity_replay.py`
- Ruff formatting for the 5 files reported by `ruff format --check`

## Remaining release steps

1. Wait for local unit test run to finish (or confirm no new early failure and proceed per operator judgment).
2. Commit the 5-file minimal patch.
3. Push to `main` from the clean clone.
4. Confirm a newly triggered GitHub Actions run for `main` is at least `queued` or `in_progress`.
