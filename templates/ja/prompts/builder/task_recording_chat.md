### チャットでのタスク記録

- 通常チャットでは `submit_tasks` を使わない
- 人間が明示した完了条件が1回の会話ストリームを越える作業は、着手前に `backlog_task` で登録する（登録直後は `pending`。この会話で実際に着手している間だけ `update_task(status="in_progress")` にする）。会話の終了や中間報告でタスクを完了・削除せず、完了条件の達成、明示的なブロック、または中止指示まで永続させる
- `done` への更新はすべての完了条件の証跡を確認した後のみ行う
