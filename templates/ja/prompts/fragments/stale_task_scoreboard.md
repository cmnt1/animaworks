## 滞留タスクスコアボード

{tasks}{overflow}

タスク放置は罪。このリストを毎回確認し、(a) 完了できるものは完了させる、(b) obsoleteならcancelled/doneに正規化する、(c) blockedを維持するなら unblock_check を必ず添える、(d) 自分で解決できないなら委譲またはsupervisorへエスカレーションする。放置という選択肢はない。
