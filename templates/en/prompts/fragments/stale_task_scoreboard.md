## Stale Task Scoreboard

{tasks}{overflow}

Leaving tasks unattended is unacceptable. Review this list every time and: (a) finish anything you can complete, (b) normalize obsolete tasks to cancelled/done, (c) always attach an unblock_check if a task remains blocked, and (d) delegate or escalate to your supervisor if you cannot resolve it yourself. Leaving it unattended is not an option.
